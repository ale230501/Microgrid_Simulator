# Legacy Wrapper Folder

`test_sweep/` is kept only for backward compatibility.

- Canonical sweep scripts: `scripts/sweeps/`

Files here are wrappers that forward execution to `scripts/sweeps/`.
Add new sweep scripts only under `scripts/sweeps/`.
