# Legacy Wrapper Folder

`Simulation_Scripts/` is kept only for backward compatibility.

- Canonical train scripts: `scripts/train/`
- Canonical test scripts: `scripts/test/`

Files here are thin wrappers that forward execution to `scripts/`.
New scripts should be added only under `scripts/`.
